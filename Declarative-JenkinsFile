pipeline {
    agent any
    
    tools{
        maven "maven"
    }

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/siddiquimahevishfatema/maven-web-app.git'
            }
        }
         stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
